type AppointmentStorage {
  id: Int!
  user: String!
  time: String!
  status: String!
}

type Query {
  appointmentRecord(id: Int!): AppointmentStorage
  getAppointment(id: Int!): AppointmentStorage
}